# IMPORTANT: This is a template. Copy to .env and fill in your actual values.
# NEVER commit the actual .env file with real credentials to version control!

# ========== DJANGO CORE SETTINGS ==========
# Generate a new SECRET_KEY at: https://djecrety.ir/
SECRET_KEY=your-secret-key-here

# Set to False for production deployment
DEBUG=False

# For production, list your actual domains
# Example: ALLOWED_HOSTS=pacificmart.com,www.pacificmart.com,your-render-app.onrender.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Optional: CSRF trusted origins for cross-domain requests
# CSRF_TRUSTED_ORIGINS=https://your-frontend-domain.com

# ========== DATABASE CONFIGURATION ==========
# For production on Render, use PostgreSQL connection string from Render dashboard
# Example: postgresql://username:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@localhost:5432/pacificmart_db

# ========== CLOUDINARY CONFIGURATION ==========
# Get these from: https://cloudinary.com/console/settings
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ========== EMAIL CONFIGURATION ==========
# For production, use SendGrid or AWS SES instead of Gmail
# Gmail app passwords reset every 30 days
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True

# ========== OPTIONAL: SECURITY SETTINGS ==========
# For production deployment
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
